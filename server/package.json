{
  "name": "express-typescript-babel",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "clean": "rm -rf build && rm -rf dist",
    "tsc": "node ./node_modules/.bin/tsc",
    "babel": "node ./node_modules/.bin/babel --presets es2015 build --out-dir dist --source-maps",
    "build": "yarn run clean && yarn run tsc && yarn run babel && cp package.json dist/ && cp -r public dist/",
    "copy-dotenv": "cp .env dist/",
    "start-dev": "NODE_ENV=development yarn run build && yarn run copy-dotenv && cd dist && NODE_ENV=development yarn start",
    "package": "zip -r pachamama-server.zip dist",
    "test": "yarn run build && yarn run copy-dotenv && cd dist && NODE_ENV=test yarn run reset-database-test && cd database && NODE_ENV=test ../node_modules/mocha/bin/mocha ../**/*.mocha.js",
    "reset-database-test": "NODE_ENV=test knex migrate:rollback --knexfile database/knexfile.js && NODE_ENV=test knex migrate:latest --knexfile database/knexfile.js && NODE_ENV=test knex seed:run --knexfile database/knexfile.js",
    "reset-database-dev": "NODE_ENV=development knex migrate:rollback --knexfile database/knexfile.js && NODE_ENV=development knex migrate:latest --knexfile database/knexfile.js && NODE_ENV=development knex seed:run --knexfile database/knexfile.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "~1.17.1",
    "connect-session-knex": "^1.3.4",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.3",
    "dotenv": "^4.0.0",
    "express": "~4.15.2",
    "express-session": "^1.15.2",
    "knex": "^0.12.9",
    "morgan": "~1.8.1",
    "passport": "^0.3.2",
    "passport-local": "^1.0.0",
    "pg": "^6.1.5",
    "serve-favicon": "~2.4.2"
  },
  "devDependencies": {
    "@types/body-parser": "^1.16.3",
    "@types/chai": "^3.4.35",
    "@types/cookie-parser": "^1.3.30",
    "@types/debug": "^0.0.29",
    "@types/dotenv": "^2.0.20",
    "@types/express": "^4.0.35",
    "@types/express-session": "^0.0.32",
    "@types/knex": "^0.0.44",
    "@types/mocha": "^2.2.40",
    "@types/morgan": "^1.7.32",
    "@types/node": "^7.0.12",
    "@types/passport": "^0.3.3",
    "@types/passport-local": "^1.0.29",
    "@types/pg": "^6.1.38",
    "@types/serve-favicon": "^2.2.28",
    "@types/sqlite3": "^2.2.33",
    "@types/tape": "^4.2.29",
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "blue-tape": "^1.0.0",
    "chai": "^3.5.0",
    "copyfiles": "^1.2.0",
    "mocha": "^3.2.0",
    "mock-knex": "^0.3.8",
    "sqlite3": "^3.1.8",
    "ts-node": "^3.0.2",
    "typescript": "^2.2.2"
  }
}
